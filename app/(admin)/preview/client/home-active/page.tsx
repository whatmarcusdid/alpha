import { ClientHomePreviewShell } from '@/components/preview/ClientHomePreviewShell';

export default function ClientHomeActivePreviewPage() {
  return (
    <ClientHomePreviewShell
      state="active"
      designQuestion="Speed Fix in progress — Work Started milestone complete with one client update in the feed."
    />
  );
}
