import { ClientHomePreviewShell } from '@/components/preview/ClientHomePreviewShell';

export default function ClientHomeDeliveredPreviewPage() {
  return (
    <ClientHomePreviewShell
      state="delivered"
      designQuestion="Fix delivered — all milestones complete with report and Loom deliverables visible."
    />
  );
}
