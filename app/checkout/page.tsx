import { redirect } from 'next/navigation';

/**
 * Subscription checkout disabled (Phase 1).
 * Component files remain in repo for a future cleanup pass.
 */
export default function CheckoutPage() {
  redirect('/');
}
