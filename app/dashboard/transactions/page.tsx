import { redirect } from 'next/navigation';

/**
 * Subscription billing UI disabled (Phase 1).
 * Component files remain in repo for a future cleanup pass.
 */
export default function TransactionsPage() {
  redirect('/dashboard');
}
