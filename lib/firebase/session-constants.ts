/** Edge-safe session cookie constants (no Firebase Admin imports). */

export const SESSION_COOKIE_NAME = '__session';

/** Firebase session cookie lifetime — 5 days. */
export const SESSION_COOKIE_MAX_AGE_MS = 60 * 60 * 24 * 5 * 1000;
